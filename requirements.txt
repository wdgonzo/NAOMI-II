# NAOMI-II Parser Demo
# The parser uses only the Python standard library -- no packages needed.
# These are optional dependencies for the full research pipeline (prior_work/).

# Core scientific computing (prior_work only)
numpy>=1.24.0
scipy>=1.10.0
networkx>=3.0

# NLP toolkit (prior_work only -- WordNet integration)
nltk>=3.8

# Visualization (prior_work only)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Machine Learning utilities (prior_work only)
scikit-learn>=1.2.0

# Data processing (prior_work only)
pandas>=2.0.0
tqdm>=4.65.0

# Deep Learning (prior_work only -- embedding training)
torch>=2.0.0
