# Demo dependencies (required)
matplotlib>=3.7.0
networkx>=3.0
nltk>=3.8

# Research/training dependencies (optional — only needed for prior_work/)
# pip install numpy scipy torch scikit-learn pandas tqdm seaborn plotly
